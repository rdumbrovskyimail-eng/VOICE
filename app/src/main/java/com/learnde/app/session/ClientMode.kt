package com.learnde.app.session

/**
 * Режим клиента.
 *  NORMAL  — «дворовой», чистый инкогнито: ничего не сохраняется, привязка к промпту сессии.
 *  CAM     — режим камеры.
 */
enum class ClientMode { NORMAL, CAM }
